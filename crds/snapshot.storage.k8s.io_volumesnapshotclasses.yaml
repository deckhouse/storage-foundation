---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    api-approved.kubernetes.io: "https://github.com/kubernetes-csi/external-snapshotter/pull/814"
    controller-gen.kubebuilder.io/version: v0.15.0
  name: volumesnapshotclasses.snapshot.storage.k8s.io
  labels:
    heritage: deckhouse
    module: snapshot-controller
spec:
  group: snapshot.storage.k8s.io
  names:
    kind: VolumeSnapshotClass
    listKind: VolumeSnapshotClassList
    plural: volumesnapshotclasses
    shortNames:
    - vsclass
    - vsclasses
    singular: volumesnapshotclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .driver
      name: Driver
      type: string
    - description: Controls whether the VolumeSnapshotContent and its physical snapshot are deleted when the bound VolumeSnapshot is deleted.
      jsonPath: .deletionPolicy
      name: DeletionPolicy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          VolumeSnapshotClass defines parameters for volume snapshot creation used
          by the underlying storage system. To use, reference by name
          in a VolumeSnapshot object.
          Non-namespaced resource.
        properties:
          apiVersion:
            description: |-
              API version of this object representation. Servers convert recognized schemas
              to the latest internal value and may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          deletionPolicy:
            description: |-
              Controls the deletion behavior of VolumeSnapshotContent and its physical snapshot
              when the bound VolumeSnapshot is deleted.
              Supported values:

              - `Retain`: VolumeSnapshotContent and its physical snapshot on the underlying storage system are preserved.
              - `Delete`: VolumeSnapshotContent and its physical snapshot on the underlying storage system are removed.
            enum:
            - Delete
            - Retain
            type: string
          driver:
            description: |-
              Name of the storage driver that handles this VolumeSnapshotClass.
            type: string
          kind:
            description: |-
              Kind of REST resource this object represents. Servers may infer this value
              from the endpoint the client submits requests to. Cannot be updated
              after creation. Value must be in CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          parameters:
            additionalProperties:
              type: string
            description: |-
              Key-value map of driver-specific parameters used when creating snapshots.
              Parameters are not interpreted by Kubernetes.
            type: object
        required:
        - deletionPolicy
        - driver
        type: object
    served: true
    storage: true
    subresources: {}
  - additionalPrinterColumns:
    - jsonPath: .driver
      name: Driver
      type: string
    - description: Controls whether the VolumeSnapshotContent and its physical snapshot are deleted when the bound VolumeSnapshot is deleted.
      jsonPath: .deletionPolicy
      name: DeletionPolicy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    # This indicates the v1beta1 version of the custom resource is deprecated.
    # API requests to this version receive a warning in the server response.
    deprecated: true
    # This overrides the default warning returned to clients making v1beta1 API requests.
    deprecationWarning: "snapshot.storage.k8s.io/v1beta1 VolumeSnapshotClass is deprecated; use snapshot.storage.k8s.io/v1 VolumeSnapshotClass"
    schema:
      openAPIV3Schema:
        description: |-
          VolumeSnapshotClass defines parameters for volume snapshot creation used
          by the underlying storage system. To use, reference by name
          in a VolumeSnapshot object.
          Non-namespaced resource.
        properties:
          apiVersion:
            description: |-
              API version of this object representation. Servers convert recognized schemas
              to the latest internal value and may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          deletionPolicy:
            description: |-
              Controls the deletion behavior of VolumeSnapshotContent and its physical snapshot
              when the bound VolumeSnapshot is deleted.
              Supported values:

              - `Retain`: VolumeSnapshotContent and its physical snapshot on the underlying storage system are preserved.
              - `Delete`: VolumeSnapshotContent and its physical snapshot on the underlying storage system are removed.
            enum:
            - Delete
            - Retain
            type: string
          driver:
            description: |-
              Name of the storage driver that handles this VolumeSnapshotClass.
            type: string
          kind:
            description: |-
              Kind of REST resource this object represents. Servers may infer this value
              from the endpoint the client submits requests to. Cannot be updated
              after creation. Value must be in CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          parameters:
            additionalProperties:
              type: string
            description: |-
              Key-value map of driver-specific parameters used when creating snapshots.
              Parameters are not interpreted by Kubernetes.
            type: object
        required:
        - deletionPolicy
        - driver
        type: object
    served: true
    storage: false
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
