---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
    module: storage-foundation
  name: volumerestorerequests.storage-foundation.deckhouse.io
spec:
  group: storage-foundation.deckhouse.io
  names:
    kind: VolumeRestoreRequest
    listKind: VolumeRestoreRequestList
    plural: volumerestorerequests
    singular: volumerestorerequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeRestoreRequest is the Schema for the volumerestorerequests
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              VolumeRestoreRequestSpec defines the desired state of VolumeRestoreRequest.

              The suffix convention is authoritative: sourceRef (Ref) points at an object that already EXISTS;
              pvcTemplate (Template) is the spec of the PVC the restore CREATES. The external-provisioner
              provisions the PV from sourceRef and creates the PVC from pvcTemplate (name required), then binds
              them in the VRR namespace (restore is never cross-namespace).
            properties:
              fsType:
                description: |-
                  FsType is the filesystem type for Filesystem volumes (optional, ignored for Block). It is a restore
                  execution parameter read by the external-provisioner, not a PVC field, so it stays at spec root.
                type: string
              pvcTemplate:
                description: |-
                  PvcTemplate is the spec of the PersistentVolumeClaim the restore creates and binds. It absorbs the
                  former root storageClassName/volumeMode/accessModes/size; the PVC name lives in
                  pvcTemplate.metadata.name (required). Namespace is implicit = the VRR namespace.
                properties:
                  metadata:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      name:
                        type: string
                    type: object
                  spec:
                    properties:
                      accessModes:
                        items:
                          type: string
                        type: array
                      resources:
                        description: VolumeResourceRequirements describes the storage
                          resource requirements for a volume.
                        properties:
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: ResourceList is a set of (resource name,
                              quantity) pairs.
                            type: object
                        type: object
                      storageClassName:
                        type: string
                      volumeMode:
                        description: PersistentVolumeMode describes how a volume is
                          intended to be consumed, either Block or Filesystem.
                        type: string
                    type: object
                type: object
              sourceRef:
                description: SourceRef references the source data to restore from
                  (VolumeSnapshotContent or PersistentVolume).
                properties:
                  apiVersion:
                    description: |-
                      APIVersion is the group/version of the referenced object (e.g., "snapshot.storage.k8s.io/v1").
                      Optional: kind-only refs (e.g. core PersistentVolumeClaim) leave it empty. It is required for
                      snapshot-leaf refs (spec.snapshotRef) so the state-snapshotter reverse-lookup can derive the group.
                    type: string
                  kind:
                    description: Kind is the kind of the referenced object (e.g.,
                      "VolumeSnapshotContent", "PersistentVolume")
                    type: string
                  name:
                    description: Name is the name of the referenced object
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced object
                      (optional)
                    type: string
                  uid:
                    description: |-
                      UID is the unique identifier of the referenced object. It is populated in status references (e.g.
                      VolumeRestoreRequest status.pvcRef) so consumers can detect recreation; spec refs may leave it empty.
                    type: string
                required:
                - name
                type: object
            required:
            - pvcTemplate
            - sourceRef
            type: object
            x-kubernetes-validations:
            - message: pvcTemplate.metadata.name is required
              rule: size(self.pvcTemplate.metadata.name) > 0
          status:
            description: VolumeRestoreRequestStatus defines the observed state of
              VolumeRestoreRequest
            properties:
              completionTimestamp:
                description: CompletionTimestamp is the time when the restore request
                  completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              pvcRef:
                description: |-
                  PvcRef references the created restore target PersistentVolumeClaim. Unlike spec, the namespace and
                  uid are populated here so the status is self-contained and consumers can detect recreation.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion is the group/version of the referenced object (e.g., "snapshot.storage.k8s.io/v1").
                      Optional: kind-only refs (e.g. core PersistentVolumeClaim) leave it empty. It is required for
                      snapshot-leaf refs (spec.snapshotRef) so the state-snapshotter reverse-lookup can derive the group.
                    type: string
                  kind:
                    description: Kind is the kind of the referenced object (e.g.,
                      "VolumeSnapshotContent", "PersistentVolume")
                    type: string
                  name:
                    description: Name is the name of the referenced object
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced object
                      (optional)
                    type: string
                  uid:
                    description: |-
                      UID is the unique identifier of the referenced object. It is populated in status references (e.g.
                      VolumeRestoreRequest status.pvcRef) so consumers can detect recreation; spec refs may leave it empty.
                    type: string
                required:
                - name
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
