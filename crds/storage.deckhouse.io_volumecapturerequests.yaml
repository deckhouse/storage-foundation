---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: volumecapturerequests.storage.deckhouse.io
spec:
  group: storage.deckhouse.io
  names:
    kind: VolumeCaptureRequest
    listKind: VolumeCaptureRequestList
    plural: volumecapturerequests
    singular: volumecapturerequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeCaptureRequest is the Schema for the volumecapturerequests
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeCaptureRequestSpec defines the desired state of VolumeCaptureRequest
            properties:
              mode:
                description: 'Mode specifies the capture mode: "Snapshot" or "Detach"'
                enum:
                - Snapshot
                - Detach
                type: string
              targets:
                description: Targets lists PVC/volume targets to capture in this request
                  (bulk, one VCR per logical snapshot node).
                items:
                  description: VolumeCaptureTarget identifies a PVC (or future volume
                    target) to capture.
                  properties:
                    apiVersion:
                      minLength: 1
                      type: string
                    kind:
                      minLength: 1
                      type: string
                    name:
                      minLength: 1
                      type: string
                    namespace:
                      minLength: 1
                      type: string
                    uid:
                      description: UID is the map key for spec.targets (PersistentVolumeClaim
                        UID).
                      minLength: 1
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - namespace
                  - uid
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - uid
                x-kubernetes-list-type: map
            required:
            - mode
            type: object
          status:
            description: VolumeCaptureRequestStatus defines the observed state of
              VolumeCaptureRequest
            properties:
              completionTimestamp:
                description: CompletionTimestamp is the time when the capture request
                  completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dataRefs:
                description: DataRefs lists per-target durable data artifacts (for
                  example VolumeSnapshotContent).
                items:
                  description: VolumeDataBinding associates a capture target with
                    its durable data artifact on one VCR.
                  properties:
                    artifact:
                      description: Artifact references the cluster-scoped durable
                        data artifact.
                      properties:
                        apiVersion:
                          minLength: 1
                          type: string
                        kind:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                    target:
                      description: Target identifies the PVC target captured in this
                        binding.
                      properties:
                        apiVersion:
                          minLength: 1
                          type: string
                        kind:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                        namespace:
                          minLength: 1
                          type: string
                        uid:
                          description: UID is the map key for spec.targets (PersistentVolumeClaim
                            UID).
                          minLength: 1
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      - namespace
                      - uid
                      type: object
                    targetUID:
                      description: TargetUID is the map key for status.dataRefs (matches
                        spec.targets[].uid).
                      minLength: 1
                      type: string
                  required:
                  - artifact
                  - target
                  - targetUID
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - targetUID
                x-kubernetes-list-type: map
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.targets must not be empty when mode is Snapshot
          rule: self.spec.mode != 'Snapshot' || size(self.spec.targets) > 0
    served: true
    storage: true
    subresources:
      status: {}
