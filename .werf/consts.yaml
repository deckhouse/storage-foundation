# Edition module settings, default ce
{{- $_ := set . "MODULE_EDITION" (env "MODULE_EDITION" "ce") }}

# Component versions are in oss.yaml; images use helpers from .werf/defines/oss-yaml.tmpl
# (e.g. get_oss_version_map_by_id_and_condition_key). The .k8s map is built from oss.yaml in .werf/images.yaml.
{{- $_ := set . "OssYamlPath" "/oss.yaml" }}
