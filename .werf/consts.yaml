# # Edition module settings, default ce
{{- $_ := set . "MODULE_EDITION" (env "MODULE_EDITION" "ce") }}

# # component versions
# {{- $versions := dict }}
# {{- $_ := set $versions "SNAPSHOT_CONTROLLER" "v8.4.0" }}
# {{- $_ := set . "VERSIONS" $versions }}

# component versions
{{- $versions := dict }}
{{- $_ := set . "VERSIONS" $versions }}

{{- $k8s := dict }}
{{- $k8s = set $k8s "1.20" (dict
  "csi" (dict
    "provisioner" "v5.3.0"
    "resizer" "v1.14.0"
    "snapshotter" "dev" #"v8.4.0"
    "livenessprobe" "v2.17.0"
    "registrar" "v2.15.0"
    "attacher" "v4.10.0"
  )
) }}
{{- $k8s = set $k8s "1.34" (dict
  "csi" (dict
    "provisioner" "v6.1.0"
    "resizer" "v2.0.0"    
  )
) }}
{{- $_ := set $ "k8s" $k8s }}
