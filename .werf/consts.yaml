# # Edition module settings, default ce
{{- $_ := set . "MODULE_EDITION" (env "MODULE_EDITION" "ce") }}

# component versions
{{- $versions := dict }}
{{- $_ := set . "VERSIONS" $versions }}

{{- $k8s := dict }}
{{- $k8s = set $k8s "1.25" (dict
  "csi" (dict
    "provisioner" "v5.3.0"
    "resizer" "v1.14.0"
    "snapshotter" "v8.5.0"
    "livenessprobe" "v2.18.0"
    "registrar" "v2.16.0"
    "attacher" "v4.11.0"
    "vsphere" "v3.0.3"
  )
) }}
{{- $k8s = set $k8s "1.34" (dict
  "csi" (dict
    "provisioner" "v6.2.0"
    "resizer" "v2.1.0"
  )
) }}
{{- $_ := set $ "k8s" $k8s }}
