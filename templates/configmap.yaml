---
apiVersion: v1
kind: ConfigMap
metadata:
  name: data-exporter-image
  namespace: d8-{{ .Chart.Name }}
  {{- include "helm_lib_module_labels" (list . ) | nindent 2 }}
data:
  image: {{ include "helm_lib_module_image" (list . "dataExporter") }}
