apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:use:role:viewer:storage-foundation
  labels:
    heritage: deckhouse
    module: storage-foundation
    rbac.deckhouse.io/kind: use
    rbac.deckhouse.io/aggregate-to-storage-foundation-as: user
    rbac.deckhouse.io/aggregate-to-all-as: viewer
aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.deckhouse.io/kind: use
        rbac.deckhouse.io/aggregate-to-storage-foundation-as: viewer
rules: []
