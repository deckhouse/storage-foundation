version: "2"
run:
  # No `go:` pin here. It used to hold the target language at 1.25 so the version check would pass
  # against a linter built with an older Go; the fix belongs where the binary is built (go-lint.sh
  # derives that toolchain from the modules), and pinning it here only asks the linter to analyse
  # 1.26 code as if it were 1.25.
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - dogsled
    - errcheck
    - gocritic
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unparam
    - whitespace
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - path: (.+)\.go$
        text: ST1005.*
      - path: (.+)\.go$
        text: should not use dot imports
      - path: (.+)\.go$
        text: don't use an underscore in package name
      - path: (.+)\.go$
        text: 'exported: .*'
      - path: (.+)\.go$
        text: QF1008.*
      - path: (.+)\.go$
        text: QF1003.*
      - path: (.+)\.go$
        text: SA1019.*
      - path: (.+)\.go$
        text: 'ifElseChain: .*'
      - path: (.+)\.go$
        text: .* always receives .*
      - path: (.+)\.go$
        text: result .* is never used
      - path: (.+)\.go$
        text: 'var-naming: avoid meaningless package names'
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gci
    - gofmt
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/deckhouse)
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
