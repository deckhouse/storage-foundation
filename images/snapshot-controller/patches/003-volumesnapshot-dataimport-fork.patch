diff --git a/client/apis/volumesnapshot/v1/types.go b/client/apis/volumesnapshot/v1/types.go
index 36f60dc95..f45a6e148 100644
--- a/client/apis/volumesnapshot/v1/types.go
+++ b/client/apis/volumesnapshot/v1/types.go
@@ -93,16 +93,34 @@ type VolumeSnapshotSpec struct {
 	// +optional
 	// +kubebuilder:validation:XValidation:rule="size(self) > 0",message="volumeSnapshotClassName must not be the empty string when set"
 	VolumeSnapshotClassName *string `json:"volumeSnapshotClassName,omitempty" protobuf:"bytes,2,opt,name=volumeSnapshotClassName"`
+
+	// mode selects how this VolumeSnapshot obtains its content (Capture | Import) and is immutable
+	// once set. Capture (default): dynamic CSI snapshot from spec.source. Import: the data artifact
+	// is produced by a DataImport (storage-foundation.deckhouse.io) in the same namespace, resolved
+	// via reverse-lookup (DataImport.spec.targetRef); spec.source is OMITTED (required only when
+	// mode != Import — full parity with the other snapshot kinds, where Import carries no source
+	// fields; a typed client may still serialize an empty source, which stays legal) and the upstream
+	// snapshot-controller does NOT reconcile such a VolumeSnapshot — the state-snapshotter common
+	// controller binds it (sets status). Deckhouse fork extension.
+	// +optional
+	// +kubebuilder:default=Capture
+	// +kubebuilder:validation:Enum=Capture;Import
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="mode is immutable"
+	Mode string `json:"mode,omitempty" protobuf:"bytes,3,opt,name=mode"`
 }
 
 // VolumeSnapshotSource specifies whether the underlying snapshot should be
 // dynamically taken upon creation or if a pre-existing VolumeSnapshotContent
 // object should be used.
-// Exactly one of its members must be set.
 // Members in VolumeSnapshotSource are immutable.
+//
+// Deckhouse fork: source is required only when spec.mode != Import (import VolumeSnapshots omit it —
+// the data artifact comes from a DataImport, there is no CSI source). An EMPTY source is also legal:
+// it carries the restore intent (the backup controller one-shot sets volumeSnapshotContentName from
+// empty after a successful restore).
 // +kubebuilder:validation:XValidation:rule="!has(oldSelf.persistentVolumeClaimName) || has(self.persistentVolumeClaimName)", message="persistentVolumeClaimName is required once set"
 // +kubebuilder:validation:XValidation:rule="!has(oldSelf.volumeSnapshotContentName) || has(self.volumeSnapshotContentName)", message="volumeSnapshotContentName is required once set"
-// +kubebuilder:validation:XValidation:rule="(has(self.volumeSnapshotContentName) && !has(self.persistentVolumeClaimName)) || (!has(self.volumeSnapshotContentName) && has(self.persistentVolumeClaimName))", message="exactly one of volumeSnapshotContentName and persistentVolumeClaimName must be set"
+// +kubebuilder:validation:XValidation:rule="[has(self.persistentVolumeClaimName), has(self.volumeSnapshotContentName)].filter(x, x).size() <= 1", message="source must be empty (restore intent / import mode) or exactly one of persistentVolumeClaimName and volumeSnapshotContentName must be set"
 type VolumeSnapshotSource struct {
 	// persistentVolumeClaimName specifies the name of the PersistentVolumeClaim
 	// object representing the volume from which a snapshot should be created.
@@ -124,6 +142,55 @@ type VolumeSnapshotSource struct {
 	VolumeSnapshotContentName *string `json:"volumeSnapshotContentName,omitempty" protobuf:"bytes,2,opt,name=volumeSnapshotContentName"`
 }
 
+// VolumeSnapshotDataBinding is the self-contained data binding mirrored onto an import VolumeSnapshot's
+// status.data by the state-snapshotter common controller. Its JSON wire shape is byte-identical to the
+// state-snapshotter SnapshotContent.status.data (sourceRef + artifactRef + volume metadata), so d8 reads the
+// captured-volume descriptor from the namespaced VolumeSnapshot without the cluster-scoped SnapshotContent.
+// Deckhouse fork extension.
+type VolumeSnapshotDataBinding struct {
+	// sourceRef identifies the captured PersistentVolumeClaim backing this node's data (uid is the identity).
+	SourceRef VolumeSnapshotDataSource `json:"sourceRef" protobuf:"bytes,1,opt,name=sourceRef"`
+	// artifactRef references the cluster-scoped durable data artifact (a VolumeSnapshotContent).
+	ArtifactRef VolumeSnapshotDataArtifact `json:"artifactRef" protobuf:"bytes,2,opt,name=artifactRef"`
+	// volumeMode records the source volume mode (Block or Filesystem).
+	// +optional
+	VolumeMode string `json:"volumeMode,omitempty" protobuf:"bytes,3,opt,name=volumeMode"`
+	// fsType records the source filesystem type (Filesystem volumes only).
+	// +optional
+	FsType string `json:"fsType,omitempty" protobuf:"bytes,4,opt,name=fsType"`
+	// accessModes records the source PVC access modes (e.g. ReadWriteOnce).
+	// +optional
+	AccessModes []string `json:"accessModes,omitempty" protobuf:"bytes,5,rep,name=accessModes"`
+	// storageClassName records the source StorageClass of the captured volume.
+	// +optional
+	StorageClassName string `json:"storageClassName,omitempty" protobuf:"bytes,6,opt,name=storageClassName"`
+	// size records the real allocated size of the captured volume (e.g. "10Gi").
+	// +optional
+	Size string `json:"size,omitempty" protobuf:"bytes,7,opt,name=size"`
+}
+
+// VolumeSnapshotDataSource identifies the captured PVC source of a VolumeSnapshotDataBinding.
+// Deckhouse fork extension.
+type VolumeSnapshotDataSource struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+	// +optional
+	Namespace string `json:"namespace,omitempty" protobuf:"bytes,4,opt,name=namespace"`
+	// +optional
+	UID string `json:"uid,omitempty" protobuf:"bytes,5,opt,name=uid"`
+}
+
+// VolumeSnapshotDataArtifact points to the durable data artifact of a VolumeSnapshotDataBinding
+// (for example a VolumeSnapshotContent). Deckhouse fork extension.
+type VolumeSnapshotDataArtifact struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+	// +optional
+	UID string `json:"uid,omitempty" protobuf:"bytes,4,opt,name=uid"`
+}
+
 // VolumeSnapshotStatus is the status of the VolumeSnapshot
 // Note that CreationTime, RestoreSize, ReadyToUse, and Error are in both
 // VolumeSnapshotStatus and VolumeSnapshotContentStatus. Fields in VolumeSnapshotStatus
@@ -194,6 +261,116 @@ type VolumeSnapshotStatus struct {
 	// VolumeSnapshot is a part of.
 	// +optional
 	VolumeGroupSnapshotName *string `json:"volumeGroupSnapshotName,omitempty" protobuf:"bytes,6,opt,name=volumeGroupSnapshotName"`
+
+	// boundSnapshotContentName is the name of the cluster-scoped state-snapshotter SnapshotContent
+	// (state-snapshotter.deckhouse.io) that backs this VolumeSnapshot as a logical node in a snapshot tree.
+	// It is written by the state-snapshotter common controller (in addition to the legacy
+	// boundVolumeSnapshotContentName which points at the CSI VolumeSnapshotContent). The upstream
+	// snapshot-controller preserves this field across status updates (read -> DeepCopy -> UpdateStatus)
+	// but never sets it. Deckhouse fork extension.
+	// +optional
+	BoundSnapshotContentName *string `json:"boundSnapshotContentName,omitempty" protobuf:"bytes,7,opt,name=boundSnapshotContentName"`
+
+	// data is the self-contained data binding (sourceRef + artifactRef + volume metadata) mirrored from the
+	// backing state-snapshotter SnapshotContent.status.data. Its wire shape is byte-identical to the
+	// domain data leaves, so d8 resolves the captured-volume descriptor from this namespaced VolumeSnapshot
+	// alone. Written by the state-snapshotter common controller (import flow). Deckhouse fork extension.
+	// +optional
+	Data *VolumeSnapshotDataBinding `json:"data,omitempty" protobuf:"bytes,8,opt,name=data"`
+
+	// captureState collects the state-snapshotter capture signals mirrored onto this VolumeSnapshot when it
+	// is a domain snapshot node: commonController holds the core-written capture-leg latches, and
+	// domainSpecificController holds the domain-written planning refs and lifecycle. The upstream
+	// snapshot-controller never sets it, but MUST preserve it across its own status updates
+	// (Get -> DeepCopy -> UpdateStatus), so it is typed here. Deckhouse fork extension.
+	// +optional
+	CaptureState *VolumeSnapshotCaptureState `json:"captureState,omitempty" protobuf:"bytes,9,opt,name=captureState"`
+
+	// childrenSnapshotRefs are the direct child snapshot edges when this VolumeSnapshot participates in a
+	// snapshot tree. A VolumeSnapshot is a data leaf, so this is normally empty; it is typed for round-trip
+	// preservation and uniformity across snapshot kinds. Deckhouse fork extension.
+	// +optional
+	ChildrenSnapshotRefs []VolumeSnapshotChildReference `json:"childrenSnapshotRefs,omitempty" protobuf:"bytes,10,rep,name=childrenSnapshotRefs"`
+
+	// sourceRef is the full reference to the captured live source object (the source PVC), written by
+	// the state-snapshotter domain controller (PublishSnapshotSource) and preserved here across the fork's
+	// status updates. Deckhouse fork extension.
+	// +optional
+	SourceRef *VolumeSnapshotSourceReference `json:"sourceRef,omitempty" protobuf:"bytes,11,opt,name=sourceRef"`
+
+	// conditions report the state-snapshotter protocol readiness (conditions[Ready] is the user-facing
+	// condition derived by the core), distinct from the CSI readyToUse binding signal. Written by the core
+	// and preserved here across the fork's status updates. Deckhouse fork extension.
+	// +optional
+	Conditions []metav1.Condition `json:"conditions,omitempty" protobuf:"bytes,12,rep,name=conditions"`
+}
+
+// VolumeSnapshotCaptureState mirrors the state-snapshotter captureState onto a domain VolumeSnapshot node.
+// The upstream snapshot-controller never sets it; it is typed only so the fork preserves it across its own
+// status round-trips (Get -> DeepCopy -> UpdateStatus). Deckhouse fork extension.
+type VolumeSnapshotCaptureState struct {
+	// +optional
+	CommonController *VolumeSnapshotCommonCaptureState `json:"commonController,omitempty" protobuf:"bytes,1,opt,name=commonController"`
+	// +optional
+	DomainSpecificController *VolumeSnapshotDomainCaptureState `json:"domainSpecificController,omitempty" protobuf:"bytes,2,opt,name=domainSpecificController"`
+}
+
+// VolumeSnapshotCommonCaptureState holds the state-snapshotter core-written capture-leg success latches.
+// Deckhouse fork extension.
+type VolumeSnapshotCommonCaptureState struct {
+	// +optional
+	ManifestCaptured *bool `json:"manifestCaptured,omitempty" protobuf:"varint,1,opt,name=manifestCaptured"`
+	// +optional
+	DataCaptured *bool `json:"dataCaptured,omitempty" protobuf:"varint,2,opt,name=dataCaptured"`
+	// +optional
+	SubtreeManifestsPersisted *bool `json:"subtreeManifestsPersisted,omitempty" protobuf:"varint,3,opt,name=subtreeManifestsPersisted"`
+	// +optional
+	SubtreePlanned *bool `json:"subtreePlanned,omitempty" protobuf:"varint,4,opt,name=subtreePlanned"`
+}
+
+// VolumeSnapshotDomainCaptureState holds the state-snapshotter domain-written planning refs and lifecycle.
+// Deckhouse fork extension.
+type VolumeSnapshotDomainCaptureState struct {
+	// +optional
+	Phase string `json:"phase,omitempty" protobuf:"bytes,1,opt,name=phase"`
+	// +optional
+	Reason string `json:"reason,omitempty" protobuf:"bytes,2,opt,name=reason"`
+	// +optional
+	Message string `json:"message,omitempty" protobuf:"bytes,3,opt,name=message"`
+	// +optional
+	ManifestCaptureRequestName string `json:"manifestCaptureRequestName,omitempty" protobuf:"bytes,4,opt,name=manifestCaptureRequestName"`
+	// +optional
+	VolumeCaptureRequestName string `json:"volumeCaptureRequestName,omitempty" protobuf:"bytes,5,opt,name=volumeCaptureRequestName"`
+	// +optional
+	ExcludedRefs []VolumeSnapshotExcludedObjectReference `json:"excludedRefs,omitempty" protobuf:"bytes,6,rep,name=excludedRefs"`
+}
+
+// VolumeSnapshotChildReference is one direct child snapshot edge of a domain VolumeSnapshot node.
+// Deckhouse fork extension.
+type VolumeSnapshotChildReference struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+}
+
+// VolumeSnapshotExcludedObjectReference is one source object excluded from a domain VolumeSnapshot node.
+// Deckhouse fork extension.
+type VolumeSnapshotExcludedObjectReference struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+}
+
+// VolumeSnapshotSourceReference is the full reference to the captured live source object (the source PVC)
+// of a domain VolumeSnapshot node. Deckhouse fork extension.
+type VolumeSnapshotSourceReference struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+	// +optional
+	Namespace string `json:"namespace,omitempty" protobuf:"bytes,4,opt,name=namespace"`
+	// +optional
+	UID string `json:"uid,omitempty" protobuf:"bytes,5,opt,name=uid"`
 }
 
 // +genclient
diff --git a/client/apis/volumesnapshot/v1/zz_generated.deepcopy.go b/client/apis/volumesnapshot/v1/zz_generated.deepcopy.go
index a590aef06..a2dca7f2d 100644
--- a/client/apis/volumesnapshot/v1/zz_generated.deepcopy.go
+++ b/client/apis/volumesnapshot/v1/zz_generated.deepcopy.go
@@ -23,6 +23,7 @@ package v1
 
 import (
 	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	runtime "k8s.io/apimachinery/pkg/runtime"
 )
 
@@ -427,9 +428,186 @@ func (in *VolumeSnapshotStatus) DeepCopyInto(out *VolumeSnapshotStatus) {
 		*out = new(string)
 		**out = **in
 	}
+	if in.BoundSnapshotContentName != nil {
+		in, out := &in.BoundSnapshotContentName, &out.BoundSnapshotContentName
+		*out = new(string)
+		**out = **in
+	}
+	if in.Data != nil {
+		in, out := &in.Data, &out.Data
+		*out = new(VolumeSnapshotDataBinding)
+		(*in).DeepCopyInto(*out)
+	}
+	if in.CaptureState != nil {
+		in, out := &in.CaptureState, &out.CaptureState
+		*out = new(VolumeSnapshotCaptureState)
+		(*in).DeepCopyInto(*out)
+	}
+	if in.ChildrenSnapshotRefs != nil {
+		in, out := &in.ChildrenSnapshotRefs, &out.ChildrenSnapshotRefs
+		*out = make([]VolumeSnapshotChildReference, len(*in))
+		copy(*out, *in)
+	}
+	if in.SourceRef != nil {
+		in, out := &in.SourceRef, &out.SourceRef
+		*out = new(VolumeSnapshotSourceReference)
+		**out = **in
+	}
+	if in.Conditions != nil {
+		in, out := &in.Conditions, &out.Conditions
+		*out = make([]metav1.Condition, len(*in))
+		for i := range *in {
+			(*in)[i].DeepCopyInto(&(*out)[i])
+		}
+	}
 	return
 }
 
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotCaptureState) DeepCopyInto(out *VolumeSnapshotCaptureState) {
+	*out = *in
+	if in.CommonController != nil {
+		in, out := &in.CommonController, &out.CommonController
+		*out = new(VolumeSnapshotCommonCaptureState)
+		(*in).DeepCopyInto(*out)
+	}
+	if in.DomainSpecificController != nil {
+		in, out := &in.DomainSpecificController, &out.DomainSpecificController
+		*out = new(VolumeSnapshotDomainCaptureState)
+		(*in).DeepCopyInto(*out)
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotCaptureState.
+func (in *VolumeSnapshotCaptureState) DeepCopy() *VolumeSnapshotCaptureState {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotCaptureState)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotCommonCaptureState) DeepCopyInto(out *VolumeSnapshotCommonCaptureState) {
+	*out = *in
+	if in.ManifestCaptured != nil {
+		in, out := &in.ManifestCaptured, &out.ManifestCaptured
+		*out = new(bool)
+		**out = **in
+	}
+	if in.DataCaptured != nil {
+		in, out := &in.DataCaptured, &out.DataCaptured
+		*out = new(bool)
+		**out = **in
+	}
+	if in.SubtreeManifestsPersisted != nil {
+		in, out := &in.SubtreeManifestsPersisted, &out.SubtreeManifestsPersisted
+		*out = new(bool)
+		**out = **in
+	}
+	if in.SubtreePlanned != nil {
+		in, out := &in.SubtreePlanned, &out.SubtreePlanned
+		*out = new(bool)
+		**out = **in
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotCommonCaptureState.
+func (in *VolumeSnapshotCommonCaptureState) DeepCopy() *VolumeSnapshotCommonCaptureState {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotCommonCaptureState)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotDomainCaptureState) DeepCopyInto(out *VolumeSnapshotDomainCaptureState) {
+	*out = *in
+	if in.ExcludedRefs != nil {
+		in, out := &in.ExcludedRefs, &out.ExcludedRefs
+		*out = make([]VolumeSnapshotExcludedObjectReference, len(*in))
+		copy(*out, *in)
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotDomainCaptureState.
+func (in *VolumeSnapshotDomainCaptureState) DeepCopy() *VolumeSnapshotDomainCaptureState {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotDomainCaptureState)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotChildReference) DeepCopyInto(out *VolumeSnapshotChildReference) {
+	*out = *in
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotChildReference.
+func (in *VolumeSnapshotChildReference) DeepCopy() *VolumeSnapshotChildReference {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotChildReference)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotExcludedObjectReference) DeepCopyInto(out *VolumeSnapshotExcludedObjectReference) {
+	*out = *in
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotExcludedObjectReference.
+func (in *VolumeSnapshotExcludedObjectReference) DeepCopy() *VolumeSnapshotExcludedObjectReference {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotExcludedObjectReference)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotSourceReference) DeepCopyInto(out *VolumeSnapshotSourceReference) {
+	*out = *in
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotSourceReference.
+func (in *VolumeSnapshotSourceReference) DeepCopy() *VolumeSnapshotSourceReference {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotSourceReference)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotDataBinding) DeepCopyInto(out *VolumeSnapshotDataBinding) {
+	*out = *in
+	if in.AccessModes != nil {
+		in, out := &in.AccessModes, &out.AccessModes
+		*out = make([]string, len(*in))
+		copy(*out, *in)
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotDataBinding.
+func (in *VolumeSnapshotDataBinding) DeepCopy() *VolumeSnapshotDataBinding {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotDataBinding)
+	in.DeepCopyInto(out)
+	return out
+}
+
 // DeepCopy is an autogenerated deepcopy function, copying the receiver, creating a new VolumeSnapshotStatus.
 func (in *VolumeSnapshotStatus) DeepCopy() *VolumeSnapshotStatus {
 	if in == nil {
diff --git a/pkg/common-controller/snapshot_controller.go b/pkg/common-controller/snapshot_controller.go
index 8c1ba60b5..f80bfe95f 100644
--- a/pkg/common-controller/snapshot_controller.go
+++ b/pkg/common-controller/snapshot_controller.go
@@ -176,6 +176,15 @@ func (ctrl *csiSnapshotCommonController) syncContent(content *crdv1.VolumeSnapsh
 func (ctrl *csiSnapshotCommonController) syncSnapshot(ctx context.Context, snapshot *crdv1.VolumeSnapshot) error {
 	klog.V(5).Infof("synchronizing VolumeSnapshot[%s]: %s", utils.SnapshotKey(snapshot), utils.GetSnapshotStatusForLogging(snapshot))
 
+	// Deckhouse fork: VolumeSnapshots that carry the import marker (spec.source.import) are owned by
+	// the state-snapshotter common controller (import flow). The snapshot-controller MUST NOT reconcile
+	// them: they have no CSI source, would fail source validation, and it would fight the
+	// common controller over status. Skip entirely (no finalizers, no status writes).
+	if snapshot.Spec.Mode == "Import" {
+		klog.V(5).Infof("syncSnapshot[%s]: skipping import-mode VolumeSnapshot (owned by state-snapshotter)", utils.SnapshotKey(snapshot))
+		return nil
+	}
+
 	klog.V(5).Infof("syncSnapshot [%s]: check if we should remove finalizer on snapshot PVC source and remove it if we can", utils.SnapshotKey(snapshot))
 
 	// Check if we should remove finalizer on PVC and remove it if we can.
@@ -469,6 +478,40 @@ func (ctrl *csiSnapshotCommonController) addVolumeGroupSnapshotOwnership(ctx con
 func (ctrl *csiSnapshotCommonController) syncUnreadySnapshot(snapshot *crdv1.VolumeSnapshot) error {
 	uniqueSnapshotName := utils.SnapshotKey(snapshot)
 	klog.V(5).Infof("syncUnreadySnapshot %s", uniqueSnapshotName)
+
+	// Deckhouse fork: stamp the "taken into work" label so the state-snapshotter VolumeSnapshot domain
+	// controller can adopt this VolumeSnapshot (fork-label discriminator, state-snapshotter design §11.1/§11.3).
+	// This is the unready path (!ready || !bound); import VolumeSnapshots (spec.source.import) never reach here
+	// because syncSnapshot/syncSnapshotByKey already skip them upstream, so the label is never stamped on
+	// import VSs. Check-then-set is idempotent across resyncs; on conflict the sync just requeues and re-checks.
+	// The label lives on the object itself, so it survives module disable/enable with no missed windows.
+	const stateSnapshotterProcessedLabelKey = "storage-foundation.deckhouse.io/processed"
+	if snapshot.ObjectMeta.Labels[stateSnapshotterProcessedLabelKey] == "" {
+		var patches []utils.PatchOp
+		if snapshot.ObjectMeta.Labels == nil {
+			patches = append(patches, utils.PatchOp{
+				Op:    "add",
+				Path:  "/metadata/labels",
+				Value: map[string]string{stateSnapshotterProcessedLabelKey: "true"},
+			})
+		} else {
+			// JSON Pointer escaping of stateSnapshotterProcessedLabelKey: "/" -> "~1" (RFC 6901).
+			patches = append(patches, utils.PatchOp{
+				Op:    "add",
+				Path:  "/metadata/labels/storage-foundation.deckhouse.io~1processed",
+				Value: "true",
+			})
+		}
+		updatedSnapshot, patchErr := utils.PatchVolumeSnapshot(snapshot, patches, ctrl.clientset)
+		if patchErr != nil {
+			return newControllerUpdateError(uniqueSnapshotName, patchErr.Error())
+		}
+		if _, storeErr := ctrl.storeSnapshotUpdate(updatedSnapshot); storeErr != nil {
+			klog.Errorf("syncUnreadySnapshot[%s]: failed to update snapshot store after stamping processed label: %v", uniqueSnapshotName, storeErr)
+		}
+		snapshot = updatedSnapshot
+	}
+
 	driverName, err := ctrl.getSnapshotDriverName(snapshot)
 	if err != nil {
 		klog.Errorf("failed to getSnapshotDriverName while recording metrics for snapshot %q: %s", utils.SnapshotKey(snapshot), err)
diff --git a/pkg/common-controller/snapshot_controller_base.go b/pkg/common-controller/snapshot_controller_base.go
index a489b9241..972446a8f 100644
--- a/pkg/common-controller/snapshot_controller_base.go
+++ b/pkg/common-controller/snapshot_controller_base.go
@@ -386,6 +386,13 @@ func (ctrl *csiSnapshotCommonController) syncSnapshotByKey(key string) error {
 	}
 	snapshot, err := ctrl.snapshotLister.VolumeSnapshots(namespace).Get(name)
 	if err == nil {
+		// Deckhouse fork: import-mode VolumeSnapshots (spec.mode: Import) are owned by the
+		// state-snapshotter common controller. Skip them before snapshot-class resolution (which would
+		// fail with no PVC source) and before any reconcile/finalizer handling.
+		if snapshot.Spec.Mode == "Import" {
+			klog.V(5).Infof("syncSnapshotByKey[%s]: skipping import-mode VolumeSnapshot (owned by state-snapshotter)", key)
+			return nil
+		}
 		// The volume snapshot still exists in informer cache, the event must have
 		// been add/update/sync
 		newSnapshot, err := ctrl.checkAndUpdateSnapshotClass(snapshot)
diff --git a/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/types.go b/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/types.go
index 36f60dc95..f45a6e148 100644
--- a/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/types.go
+++ b/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/types.go
@@ -93,16 +93,34 @@ type VolumeSnapshotSpec struct {
 	// +optional
 	// +kubebuilder:validation:XValidation:rule="size(self) > 0",message="volumeSnapshotClassName must not be the empty string when set"
 	VolumeSnapshotClassName *string `json:"volumeSnapshotClassName,omitempty" protobuf:"bytes,2,opt,name=volumeSnapshotClassName"`
+
+	// mode selects how this VolumeSnapshot obtains its content (Capture | Import) and is immutable
+	// once set. Capture (default): dynamic CSI snapshot from spec.source. Import: the data artifact
+	// is produced by a DataImport (storage-foundation.deckhouse.io) in the same namespace, resolved
+	// via reverse-lookup (DataImport.spec.targetRef); spec.source is OMITTED (required only when
+	// mode != Import — full parity with the other snapshot kinds, where Import carries no source
+	// fields; a typed client may still serialize an empty source, which stays legal) and the upstream
+	// snapshot-controller does NOT reconcile such a VolumeSnapshot — the state-snapshotter common
+	// controller binds it (sets status). Deckhouse fork extension.
+	// +optional
+	// +kubebuilder:default=Capture
+	// +kubebuilder:validation:Enum=Capture;Import
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="mode is immutable"
+	Mode string `json:"mode,omitempty" protobuf:"bytes,3,opt,name=mode"`
 }
 
 // VolumeSnapshotSource specifies whether the underlying snapshot should be
 // dynamically taken upon creation or if a pre-existing VolumeSnapshotContent
 // object should be used.
-// Exactly one of its members must be set.
 // Members in VolumeSnapshotSource are immutable.
+//
+// Deckhouse fork: source is required only when spec.mode != Import (import VolumeSnapshots omit it —
+// the data artifact comes from a DataImport, there is no CSI source). An EMPTY source is also legal:
+// it carries the restore intent (the backup controller one-shot sets volumeSnapshotContentName from
+// empty after a successful restore).
 // +kubebuilder:validation:XValidation:rule="!has(oldSelf.persistentVolumeClaimName) || has(self.persistentVolumeClaimName)", message="persistentVolumeClaimName is required once set"
 // +kubebuilder:validation:XValidation:rule="!has(oldSelf.volumeSnapshotContentName) || has(self.volumeSnapshotContentName)", message="volumeSnapshotContentName is required once set"
-// +kubebuilder:validation:XValidation:rule="(has(self.volumeSnapshotContentName) && !has(self.persistentVolumeClaimName)) || (!has(self.volumeSnapshotContentName) && has(self.persistentVolumeClaimName))", message="exactly one of volumeSnapshotContentName and persistentVolumeClaimName must be set"
+// +kubebuilder:validation:XValidation:rule="[has(self.persistentVolumeClaimName), has(self.volumeSnapshotContentName)].filter(x, x).size() <= 1", message="source must be empty (restore intent / import mode) or exactly one of persistentVolumeClaimName and volumeSnapshotContentName must be set"
 type VolumeSnapshotSource struct {
 	// persistentVolumeClaimName specifies the name of the PersistentVolumeClaim
 	// object representing the volume from which a snapshot should be created.
@@ -124,6 +142,55 @@ type VolumeSnapshotSource struct {
 	VolumeSnapshotContentName *string `json:"volumeSnapshotContentName,omitempty" protobuf:"bytes,2,opt,name=volumeSnapshotContentName"`
 }
 
+// VolumeSnapshotDataBinding is the self-contained data binding mirrored onto an import VolumeSnapshot's
+// status.data by the state-snapshotter common controller. Its JSON wire shape is byte-identical to the
+// state-snapshotter SnapshotContent.status.data (sourceRef + artifactRef + volume metadata), so d8 reads the
+// captured-volume descriptor from the namespaced VolumeSnapshot without the cluster-scoped SnapshotContent.
+// Deckhouse fork extension.
+type VolumeSnapshotDataBinding struct {
+	// sourceRef identifies the captured PersistentVolumeClaim backing this node's data (uid is the identity).
+	SourceRef VolumeSnapshotDataSource `json:"sourceRef" protobuf:"bytes,1,opt,name=sourceRef"`
+	// artifactRef references the cluster-scoped durable data artifact (a VolumeSnapshotContent).
+	ArtifactRef VolumeSnapshotDataArtifact `json:"artifactRef" protobuf:"bytes,2,opt,name=artifactRef"`
+	// volumeMode records the source volume mode (Block or Filesystem).
+	// +optional
+	VolumeMode string `json:"volumeMode,omitempty" protobuf:"bytes,3,opt,name=volumeMode"`
+	// fsType records the source filesystem type (Filesystem volumes only).
+	// +optional
+	FsType string `json:"fsType,omitempty" protobuf:"bytes,4,opt,name=fsType"`
+	// accessModes records the source PVC access modes (e.g. ReadWriteOnce).
+	// +optional
+	AccessModes []string `json:"accessModes,omitempty" protobuf:"bytes,5,rep,name=accessModes"`
+	// storageClassName records the source StorageClass of the captured volume.
+	// +optional
+	StorageClassName string `json:"storageClassName,omitempty" protobuf:"bytes,6,opt,name=storageClassName"`
+	// size records the real allocated size of the captured volume (e.g. "10Gi").
+	// +optional
+	Size string `json:"size,omitempty" protobuf:"bytes,7,opt,name=size"`
+}
+
+// VolumeSnapshotDataSource identifies the captured PVC source of a VolumeSnapshotDataBinding.
+// Deckhouse fork extension.
+type VolumeSnapshotDataSource struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+	// +optional
+	Namespace string `json:"namespace,omitempty" protobuf:"bytes,4,opt,name=namespace"`
+	// +optional
+	UID string `json:"uid,omitempty" protobuf:"bytes,5,opt,name=uid"`
+}
+
+// VolumeSnapshotDataArtifact points to the durable data artifact of a VolumeSnapshotDataBinding
+// (for example a VolumeSnapshotContent). Deckhouse fork extension.
+type VolumeSnapshotDataArtifact struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+	// +optional
+	UID string `json:"uid,omitempty" protobuf:"bytes,4,opt,name=uid"`
+}
+
 // VolumeSnapshotStatus is the status of the VolumeSnapshot
 // Note that CreationTime, RestoreSize, ReadyToUse, and Error are in both
 // VolumeSnapshotStatus and VolumeSnapshotContentStatus. Fields in VolumeSnapshotStatus
@@ -194,6 +261,116 @@ type VolumeSnapshotStatus struct {
 	// VolumeSnapshot is a part of.
 	// +optional
 	VolumeGroupSnapshotName *string `json:"volumeGroupSnapshotName,omitempty" protobuf:"bytes,6,opt,name=volumeGroupSnapshotName"`
+
+	// boundSnapshotContentName is the name of the cluster-scoped state-snapshotter SnapshotContent
+	// (state-snapshotter.deckhouse.io) that backs this VolumeSnapshot as a logical node in a snapshot tree.
+	// It is written by the state-snapshotter common controller (in addition to the legacy
+	// boundVolumeSnapshotContentName which points at the CSI VolumeSnapshotContent). The upstream
+	// snapshot-controller preserves this field across status updates (read -> DeepCopy -> UpdateStatus)
+	// but never sets it. Deckhouse fork extension.
+	// +optional
+	BoundSnapshotContentName *string `json:"boundSnapshotContentName,omitempty" protobuf:"bytes,7,opt,name=boundSnapshotContentName"`
+
+	// data is the self-contained data binding (sourceRef + artifactRef + volume metadata) mirrored from the
+	// backing state-snapshotter SnapshotContent.status.data. Its wire shape is byte-identical to the
+	// domain data leaves, so d8 resolves the captured-volume descriptor from this namespaced VolumeSnapshot
+	// alone. Written by the state-snapshotter common controller (import flow). Deckhouse fork extension.
+	// +optional
+	Data *VolumeSnapshotDataBinding `json:"data,omitempty" protobuf:"bytes,8,opt,name=data"`
+
+	// captureState collects the state-snapshotter capture signals mirrored onto this VolumeSnapshot when it
+	// is a domain snapshot node: commonController holds the core-written capture-leg latches, and
+	// domainSpecificController holds the domain-written planning refs and lifecycle. The upstream
+	// snapshot-controller never sets it, but MUST preserve it across its own status updates
+	// (Get -> DeepCopy -> UpdateStatus), so it is typed here. Deckhouse fork extension.
+	// +optional
+	CaptureState *VolumeSnapshotCaptureState `json:"captureState,omitempty" protobuf:"bytes,9,opt,name=captureState"`
+
+	// childrenSnapshotRefs are the direct child snapshot edges when this VolumeSnapshot participates in a
+	// snapshot tree. A VolumeSnapshot is a data leaf, so this is normally empty; it is typed for round-trip
+	// preservation and uniformity across snapshot kinds. Deckhouse fork extension.
+	// +optional
+	ChildrenSnapshotRefs []VolumeSnapshotChildReference `json:"childrenSnapshotRefs,omitempty" protobuf:"bytes,10,rep,name=childrenSnapshotRefs"`
+
+	// sourceRef is the full reference to the captured live source object (the source PVC), written by
+	// the state-snapshotter domain controller (PublishSnapshotSource) and preserved here across the fork's
+	// status updates. Deckhouse fork extension.
+	// +optional
+	SourceRef *VolumeSnapshotSourceReference `json:"sourceRef,omitempty" protobuf:"bytes,11,opt,name=sourceRef"`
+
+	// conditions report the state-snapshotter protocol readiness (conditions[Ready] is the user-facing
+	// condition derived by the core), distinct from the CSI readyToUse binding signal. Written by the core
+	// and preserved here across the fork's status updates. Deckhouse fork extension.
+	// +optional
+	Conditions []metav1.Condition `json:"conditions,omitempty" protobuf:"bytes,12,rep,name=conditions"`
+}
+
+// VolumeSnapshotCaptureState mirrors the state-snapshotter captureState onto a domain VolumeSnapshot node.
+// The upstream snapshot-controller never sets it; it is typed only so the fork preserves it across its own
+// status round-trips (Get -> DeepCopy -> UpdateStatus). Deckhouse fork extension.
+type VolumeSnapshotCaptureState struct {
+	// +optional
+	CommonController *VolumeSnapshotCommonCaptureState `json:"commonController,omitempty" protobuf:"bytes,1,opt,name=commonController"`
+	// +optional
+	DomainSpecificController *VolumeSnapshotDomainCaptureState `json:"domainSpecificController,omitempty" protobuf:"bytes,2,opt,name=domainSpecificController"`
+}
+
+// VolumeSnapshotCommonCaptureState holds the state-snapshotter core-written capture-leg success latches.
+// Deckhouse fork extension.
+type VolumeSnapshotCommonCaptureState struct {
+	// +optional
+	ManifestCaptured *bool `json:"manifestCaptured,omitempty" protobuf:"varint,1,opt,name=manifestCaptured"`
+	// +optional
+	DataCaptured *bool `json:"dataCaptured,omitempty" protobuf:"varint,2,opt,name=dataCaptured"`
+	// +optional
+	SubtreeManifestsPersisted *bool `json:"subtreeManifestsPersisted,omitempty" protobuf:"varint,3,opt,name=subtreeManifestsPersisted"`
+	// +optional
+	SubtreePlanned *bool `json:"subtreePlanned,omitempty" protobuf:"varint,4,opt,name=subtreePlanned"`
+}
+
+// VolumeSnapshotDomainCaptureState holds the state-snapshotter domain-written planning refs and lifecycle.
+// Deckhouse fork extension.
+type VolumeSnapshotDomainCaptureState struct {
+	// +optional
+	Phase string `json:"phase,omitempty" protobuf:"bytes,1,opt,name=phase"`
+	// +optional
+	Reason string `json:"reason,omitempty" protobuf:"bytes,2,opt,name=reason"`
+	// +optional
+	Message string `json:"message,omitempty" protobuf:"bytes,3,opt,name=message"`
+	// +optional
+	ManifestCaptureRequestName string `json:"manifestCaptureRequestName,omitempty" protobuf:"bytes,4,opt,name=manifestCaptureRequestName"`
+	// +optional
+	VolumeCaptureRequestName string `json:"volumeCaptureRequestName,omitempty" protobuf:"bytes,5,opt,name=volumeCaptureRequestName"`
+	// +optional
+	ExcludedRefs []VolumeSnapshotExcludedObjectReference `json:"excludedRefs,omitempty" protobuf:"bytes,6,rep,name=excludedRefs"`
+}
+
+// VolumeSnapshotChildReference is one direct child snapshot edge of a domain VolumeSnapshot node.
+// Deckhouse fork extension.
+type VolumeSnapshotChildReference struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+}
+
+// VolumeSnapshotExcludedObjectReference is one source object excluded from a domain VolumeSnapshot node.
+// Deckhouse fork extension.
+type VolumeSnapshotExcludedObjectReference struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+}
+
+// VolumeSnapshotSourceReference is the full reference to the captured live source object (the source PVC)
+// of a domain VolumeSnapshot node. Deckhouse fork extension.
+type VolumeSnapshotSourceReference struct {
+	APIVersion string `json:"apiVersion" protobuf:"bytes,1,opt,name=apiVersion"`
+	Kind       string `json:"kind" protobuf:"bytes,2,opt,name=kind"`
+	Name       string `json:"name" protobuf:"bytes,3,opt,name=name"`
+	// +optional
+	Namespace string `json:"namespace,omitempty" protobuf:"bytes,4,opt,name=namespace"`
+	// +optional
+	UID string `json:"uid,omitempty" protobuf:"bytes,5,opt,name=uid"`
 }
 
 // +genclient
diff --git a/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/zz_generated.deepcopy.go b/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/zz_generated.deepcopy.go
index a590aef06..a2dca7f2d 100644
--- a/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/zz_generated.deepcopy.go
+++ b/vendor/github.com/kubernetes-csi/external-snapshotter/client/v8/apis/volumesnapshot/v1/zz_generated.deepcopy.go
@@ -23,6 +23,7 @@ package v1
 
 import (
 	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	runtime "k8s.io/apimachinery/pkg/runtime"
 )
 
@@ -427,9 +428,186 @@ func (in *VolumeSnapshotStatus) DeepCopyInto(out *VolumeSnapshotStatus) {
 		*out = new(string)
 		**out = **in
 	}
+	if in.BoundSnapshotContentName != nil {
+		in, out := &in.BoundSnapshotContentName, &out.BoundSnapshotContentName
+		*out = new(string)
+		**out = **in
+	}
+	if in.Data != nil {
+		in, out := &in.Data, &out.Data
+		*out = new(VolumeSnapshotDataBinding)
+		(*in).DeepCopyInto(*out)
+	}
+	if in.CaptureState != nil {
+		in, out := &in.CaptureState, &out.CaptureState
+		*out = new(VolumeSnapshotCaptureState)
+		(*in).DeepCopyInto(*out)
+	}
+	if in.ChildrenSnapshotRefs != nil {
+		in, out := &in.ChildrenSnapshotRefs, &out.ChildrenSnapshotRefs
+		*out = make([]VolumeSnapshotChildReference, len(*in))
+		copy(*out, *in)
+	}
+	if in.SourceRef != nil {
+		in, out := &in.SourceRef, &out.SourceRef
+		*out = new(VolumeSnapshotSourceReference)
+		**out = **in
+	}
+	if in.Conditions != nil {
+		in, out := &in.Conditions, &out.Conditions
+		*out = make([]metav1.Condition, len(*in))
+		for i := range *in {
+			(*in)[i].DeepCopyInto(&(*out)[i])
+		}
+	}
 	return
 }
 
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotCaptureState) DeepCopyInto(out *VolumeSnapshotCaptureState) {
+	*out = *in
+	if in.CommonController != nil {
+		in, out := &in.CommonController, &out.CommonController
+		*out = new(VolumeSnapshotCommonCaptureState)
+		(*in).DeepCopyInto(*out)
+	}
+	if in.DomainSpecificController != nil {
+		in, out := &in.DomainSpecificController, &out.DomainSpecificController
+		*out = new(VolumeSnapshotDomainCaptureState)
+		(*in).DeepCopyInto(*out)
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotCaptureState.
+func (in *VolumeSnapshotCaptureState) DeepCopy() *VolumeSnapshotCaptureState {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotCaptureState)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotCommonCaptureState) DeepCopyInto(out *VolumeSnapshotCommonCaptureState) {
+	*out = *in
+	if in.ManifestCaptured != nil {
+		in, out := &in.ManifestCaptured, &out.ManifestCaptured
+		*out = new(bool)
+		**out = **in
+	}
+	if in.DataCaptured != nil {
+		in, out := &in.DataCaptured, &out.DataCaptured
+		*out = new(bool)
+		**out = **in
+	}
+	if in.SubtreeManifestsPersisted != nil {
+		in, out := &in.SubtreeManifestsPersisted, &out.SubtreeManifestsPersisted
+		*out = new(bool)
+		**out = **in
+	}
+	if in.SubtreePlanned != nil {
+		in, out := &in.SubtreePlanned, &out.SubtreePlanned
+		*out = new(bool)
+		**out = **in
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotCommonCaptureState.
+func (in *VolumeSnapshotCommonCaptureState) DeepCopy() *VolumeSnapshotCommonCaptureState {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotCommonCaptureState)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotDomainCaptureState) DeepCopyInto(out *VolumeSnapshotDomainCaptureState) {
+	*out = *in
+	if in.ExcludedRefs != nil {
+		in, out := &in.ExcludedRefs, &out.ExcludedRefs
+		*out = make([]VolumeSnapshotExcludedObjectReference, len(*in))
+		copy(*out, *in)
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotDomainCaptureState.
+func (in *VolumeSnapshotDomainCaptureState) DeepCopy() *VolumeSnapshotDomainCaptureState {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotDomainCaptureState)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotChildReference) DeepCopyInto(out *VolumeSnapshotChildReference) {
+	*out = *in
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotChildReference.
+func (in *VolumeSnapshotChildReference) DeepCopy() *VolumeSnapshotChildReference {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotChildReference)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotExcludedObjectReference) DeepCopyInto(out *VolumeSnapshotExcludedObjectReference) {
+	*out = *in
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotExcludedObjectReference.
+func (in *VolumeSnapshotExcludedObjectReference) DeepCopy() *VolumeSnapshotExcludedObjectReference {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotExcludedObjectReference)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotSourceReference) DeepCopyInto(out *VolumeSnapshotSourceReference) {
+	*out = *in
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotSourceReference.
+func (in *VolumeSnapshotSourceReference) DeepCopy() *VolumeSnapshotSourceReference {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotSourceReference)
+	in.DeepCopyInto(out)
+	return out
+}
+
+// DeepCopyInto is a deepcopy function, copying the receiver, writing into out. in must be non-nil.
+func (in *VolumeSnapshotDataBinding) DeepCopyInto(out *VolumeSnapshotDataBinding) {
+	*out = *in
+	if in.AccessModes != nil {
+		in, out := &in.AccessModes, &out.AccessModes
+		*out = make([]string, len(*in))
+		copy(*out, *in)
+	}
+}
+
+// DeepCopy is a deepcopy function, copying the receiver, creating a new VolumeSnapshotDataBinding.
+func (in *VolumeSnapshotDataBinding) DeepCopy() *VolumeSnapshotDataBinding {
+	if in == nil {
+		return nil
+	}
+	out := new(VolumeSnapshotDataBinding)
+	in.DeepCopyInto(out)
+	return out
+}
+
 // DeepCopy is an autogenerated deepcopy function, copying the receiver, creating a new VolumeSnapshotStatus.
 func (in *VolumeSnapshotStatus) DeepCopy() *VolumeSnapshotStatus {
 	if in == nil {
