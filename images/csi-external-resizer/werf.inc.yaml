# Based on https://github.com/kubernetes-csi/external-resizer/blob/master/Dockerfile
{{- range $key, $rawVer := index $.k8sVersions $.ImageName }}
  {{- $version := toString $key }}
  {{- $src_version := toString $rawVer }}
  {{- $patches := (printf "/images/%s" $.ImageName) }}
  {{- $patch := false }}
  {{- range $path, $_ := $.Files.Glob (printf "%s/*" $patches) }}
    {{- if contains $src_version $path }}
      {{- $patch = true }}
    {{- end }}
  {{- end }}
---
image: {{ $.ModuleNamePrefix }}{{ $.ImageName }}-ForK8SGE-{{ $version | replace "." "-" }}
from: {{ index $.Images "builder/distroless" }}
git:
{{- include "image mount points" $ }}
import:
- image: {{ $.ModuleNamePrefix }}{{ $.ImageName }}-artifact-{{ $version | replace "." "-" }}
  add: /csi-resizer
  to: /csi-resizer
  before: setup
imageSpec:
  config:
    entrypoint: ["/csi-resizer"]
---
image: {{ $.ModuleNamePrefix }}{{ $.ImageName }}-src-artifact-{{ $version | replace "." "-" }}
from: {{ index $.Images "builder/src" }}
final: false
    {{- if $patch }}
git:
- add: {{ $patches }}/patches/{{ $src_version }}
  to: /patches
  stageDependencies:
    install:
    - '**/*'
    {{- end }}
secrets:
- id: SOURCE_REPO
  value: {{ $.SOURCE_REPO }}
shell:
  install:
  - cd /src
  - git clone --depth 1 --branch {{ $src_version }} $(cat /run/secrets/SOURCE_REPO)/kubernetes-csi/external-resizer.git .
  {{- if $patch }}
  - git apply /patches/*.patch --verbose
  {{- end }}
  - rm -rf vendor
  - rm -rf .git
---
image: {{ $.ModuleNamePrefix }}{{ $.ImageName }}-artifact-{{ $version | replace "." "-" }}
from: {{ index $.Images (eq $.SVACE_ENABLED "false" | ternary "builder/golang" "builder/alpine-svace") }}
mount:
{{ include "mount points for golang builds" $ }}
final: false
import:
- image: {{ $.ModuleNamePrefix }}{{ $.ImageName }}-src-artifact-{{ $version | replace "." "-" }}
  add: /src
  to: /src
  before: install
secrets:
- id: GOPROXY
  value: {{ $.GOPROXY }}
shell:
  install:
    {{- if eq $.SVACE_ENABLED "false" }}
    - pm install git make bash
    {{- else }}
    {{- include "alpine packages proxy" $ | nindent 4 }}
    - apk add --no-cache make bash
    {{- end }}
    - export GO_VERSION=${GOLANG_VERSION} CGO_ENABLED=0 GOOS=linux GOARCH=amd64
    - cd /src
    - GOPROXY=$(cat /run/secrets/GOPROXY) go mod download
    - go mod vendor
    - make build
    - cp bin/csi-resizer /csi-resizer
    - chown 64535:64535 /csi-resizer
    - chmod 0755 /csi-resizer

{{- /*
Attach an OpenVEX attestation to the published csi-resizer image so trivy
suppresses the unfixable golang.org/x/crypto/openpgp advisory (GO-2026-5932)
reported against the csi-resizer binary. Only the v2.1.0 build links
golang.org/x/crypto (via k8s.io/apiserver); the v1.14.0 build does not, so it
needs no attestation. Predicate:
/images/csi-external-resizer/known_vulnerabilities.vex (per the "vex mitigation" template).
*/ -}}
{{- if eq $src_version "v2.1.0" }}
{{- include "vex mitigation" (list $ (printf "%s%s-ForK8SGE-%s" $.ModuleNamePrefix $.ImageName ($version | replace "." "-"))) }}
{{- end }}
{{- end }}
