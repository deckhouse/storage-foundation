# PR-F: массовый VolumeCaptureRequest (`targets[]`, `status.dataRefs[]`)

Рабочие заметки по массовому (bulk) режиму VolumeCaptureRequest:
контракт API (`targets[]`, `status.dataRefs[]`), контроллер массовых
снапшотов и обработка артефактов. Актуальные подробности и статусы
этапов см. в англоязычной версии документа.
